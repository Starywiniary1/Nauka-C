#include <stdio.h>
#include <stdint.h>

int main(void)
{
    int8_t temperatura = 55;
    
    uint8_t alarm = czy_alarm_temperatury(temperatura);
    if (alarm)
    {
        printf("Uwaga alarm");
    }
    else
    {
        printf("Brak alarmu");    
    }

    return 0;
}
